import { StoreShell } from "@/components/StoreShell";
import { PickDetailClient } from "@/components/account/PickDetailClient";

export default function PickDetailPage() {
  return (
    <StoreShell>
      <PickDetailClient />
    </StoreShell>
  );
}
