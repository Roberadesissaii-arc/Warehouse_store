import { StoreShell } from "@/components/StoreShell";
import { PicksClient } from "@/components/account/PicksClient";

export default function OrdersPage() {
  return (
    <StoreShell>
      <PicksClient />
    </StoreShell>
  );
}
